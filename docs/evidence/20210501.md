# 20210501 release evidence

This file records lock-bound validation performed on 2026-08-27. Large kit
artifacts and complete fingerprint manifests are release artifacts; their
trust anchor is `locks/toolchain.20210501.lock`.

## Builder and inputs

- Builder release: OmniOS r151038.
- Release base: `2ed5ea5a06df7f669d20d88729c625981a0de7bc`.
- Build head: `e0b4275f346eda86b39157cd7dd3cc889a1f6988`.
- Gate compiler: gcc 7.5.0; JDK: OpenJDK 11.
- Gate version: `sysroot/20210501-0-ge0b4275f346e`.
- Archive identity: `20210501-e0b4275f34-v1`.
- Toolchain lock SHA-256:
  `ca2c59928d81771868bc0b96a5fed2946e523b5295f0d80a3d6370ebba414e99`.
- Rust: `1.94.1`; `mf2tar/Cargo.lock` SHA-256:
  `e380d06971a59c26b82f1cd824d216180f37b9e960af4ed4235f20dba12bd7f2`.
- Closed bins SHA-256: normal
  `18e82bace8481dca62586e4bdff7f6b44fc63b41443799929e4d4b2187e98535`,
  non-debug
  `da3ca1ea24972ba6a01169265d8d38a45789ed7cc45334bfe026c108b1d2ff56`.

The offline installer verified every exact requested package and the final
157-FMRI runtime closure. The gate-only input and checkout contained no Rust
source or Cargo manifest.

## Clean-build comparison

The first clean four-job nightly completed without tool, build, or package
errors. Its complete `repo.redist` fingerprint is:

```text
directories                 792
files                     23824
links                         0
all files       5b874b4cdd0d4a6cef24bbec9f9536c4d739226f10755ef08216dcb4c7c645ec
payloads        25298dfd03f69e443017259a96c5a0d42887a0542d93164c3131b54c187f4dc7
manifests       da2523569661a49bf3ccb89967c12c741155e99b358309680bf0e4254bf58059
package actions 0ec7af8c762af201e2a97d510126b965e25e264759a6c16c6e29581b141dad03
```

Linux assembly from that repository produced:

```text
tar     861b41ff781c2be41ec553bb4ea25b28aae30126911aab362143629592b65c6e
tar.gz  a60094d2a6c315b1379736de8d9980e9401562ea1541d99f11c95f1eeb204fc6
```

A second clean four-job run on the same builder is in progress. No second
independent r151038 builder is currently available.

## Published-v0 comparison

The official v0 archive SHA-256 is
`28d8f4f6d84331ff1e99ac3d68b917cf8174897a5c00171c5e493253eb1587f6`.
The deterministic rebuild uses a v1 identity because its compressed bytes do
not reproduce v0.

The extracted archives have identical path, directory, file, symlink, and
symlink-target lists: 138 directories, 1,904 files, and 571 symlinks. Of the
files, 1,510 are byte-identical and 394 differ. Every difference is
classified:

- 390 gate-built ELF files use the pinned build head and fixed November 2025
  release identity rather than v0's February 2026 build identity: 204 ELF32
  and 186 ELF64 files;
- the two amd64 shim files use the locked r151038/gcc 7 result; the i386 shims
  are byte-identical to v0;
- the two `libssp_ns.a` member payloads are byte-identical, but v0's archive
  headers contain 2026 timestamps and uid/gid 100/1 while v1 records
  deterministic zero metadata.

The v1 extracted-tree fingerprints are:

```text
files  0ef95f49775fb6f9183b8485ccc5ac6410b27788c4efc52539e500b56b211152
links  d7a60036439a32a53757dc280f901deb14081213c1bc6659126ad33fcb191338
```

## Contents, shims, and runtime

The archive contains the required headers, libraries, crt objects, both
architectures of `libssp_ns.a`, GSS libraries, and all four shim files. It
excludes every directory forbidden by the release plan, and every symlink
target resolves inside the extracted tree.

The shim SHA-256 values are:

```text
i386  libgcc_s.so.1     f62e7eb2e95a907840f1c26e1dbe312cca8d69cfc923175d43ce30d26bfad4b1
amd64 libgcc_s.so.1     7a30a34c0509ced78ecc66092912e277ddd3d64acfc92c988aa411df5770ec2f
i386  libssp.so.0.0.0   64e9888ed2bbbd6d8122361f5f5b13fd610501e57840ce62b0e686e2cde0d702
amd64 libssp.so.0.0.0   401fa18ecaabe70a068f8c8a7f2431ff8829c87f11a2132e43dae51bb3a8b35d
```

Helios `pvs` found `GCC_4.8.0` and `LIBSSP_1.0` in both architectures.
Exact-path native gcc 14 links against each 32-bit and 64-bit shim pair
produced valid Solaris executables. These shims are link-time compatibility
objects, not runtime replacements.

A Clang/LLD cross-linked amd64 smoke binary recorded exactly
`libsocket.so.1`, `libpthread.so.1`, `libm.so.2`, and `libc.so.1`. It ran on
`helios-3.0.23976` and printed `illumos-sysroot-ok`.

## Remaining acceptance gaps

- Finish and compare the second clean same-builder nightly and archive.
- Repeat the build on an independent r151038 builder when one is available.
- Run the manual hosted workflow from a pushed revision if hosted evidence is
  required for the release.
