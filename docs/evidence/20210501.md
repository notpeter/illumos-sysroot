# 20210501 release evidence

This file records lock-bound validation performed from 2026-08-27 through
2026-08-28. Large kit artifacts and complete fingerprint manifests are
release artifacts; their trust anchor is `locks/toolchain.20210501.lock`.

## Builder and inputs

- Builder release: OmniOS r151038.
- Release base: `2ed5ea5a06df7f669d20d88729c625981a0de7bc`.
- Build head: `e0b4275f346eda86b39157cd7dd3cc889a1f6988`.
- Gate compiler: gcc 7.5.0; JDK: OpenJDK 11.
- Gate version: `sysroot/20210501-0-ge0b4275f346e`.
- Archive identity: `20210501-e0b4275f34-v1`.
- Build driver SHA-256:
  `747f2ab8d2fb99f20798e583a4522018ef759e9d8dbdae34aeb484bf01343583`.
- Toolchain lock SHA-256:
  `ca2c59928d81771868bc0b96a5fed2946e523b5295f0d80a3d6370ebba414e99`.
- Rust: `1.94.1`; `mf2tar/Cargo.lock` SHA-256:
  `e380d06971a59c26b82f1cd824d216180f37b9e960af4ed4235f20dba12bd7f2`.
- Closed bins SHA-256: normal
  `18e82bace8481dca62586e4bdff7f6b44fc63b41443799929e4d4b2187e98535`,
  non-debug
  `da3ca1ea24972ba6a01169265d8d38a45789ed7cc45334bfe026c108b1d2ff56`.

The offline installer verified every exact requested package.  After the
builders quiesced, the runtime closure was recaptured: all 157 exact installed
FMRIs match `locks/toolchain.20210501.lock`.  The gate-only input and checkout
contained no Rust source or Cargo manifest.

## Clean-build comparison

Two clean four-job full nightlies (`final-build-20210501-10` and
`final-build-20210501-11`) completed without tool, build, or package errors on
the locked r151038 builder.  All eight normalized fingerprint manifests match
byte-for-byte.  Their complete `repo.redist` fingerprint is:

```text
directories                 792
files                     23824
links                         0
all files       3461baf4b70855b24ade68f78c23e1f3d746d4764ee38a50aef8c017aca7f751
payloads        f8b28c0c7ef8f7f534aeb69caa0580d6e84da3cd5a414bd423b5e6e2223c542d
manifests       538427b2a8ac439b4331fcf7d6f60281d195c002b4360405d5c6da102ec3bca6
package actions 405bb71f5c210dfcd7b104807b848b58f1d19a4c58dce01823f2dcd1d7f38bf7
```

Both final repositories were transferred to Linux; the first re-fingerprinted
with all four digests unchanged.  Assembly was run separately from each
repository, and both the uncompressed and compressed outputs are
byte-identical:

```text
tar     3c37032a15f54e3073a8c7ad67d95f77530beacc30b598fbf71f67e3e2ead0cc
tar.gz  687513e155593a8ad0932ad17957a24ec423a4fda14df5b14472a583ad5a5cf9
```

Earlier different-path diagnostics classified 79 varying package paths: 55
ELF, PE, or boot binaries embedded checkout paths, three SQLite2 seed
databases embedded manifest paths, and 21 Python 2.7 bytecode files contained
source timestamps.  Canonical compiler source/include arguments, SMF manifest
imports, recognized legacy bytecode headers, and the lexer input removed those
sources.  The final three residual binaries were traced to an attached
absolute include path (`ppt` and `vmm`) and the lex input path
(`geniconvtbl`); the final runs above prove those corrections.  No second
independent r151038 builder is currently available.

## Published-v0 comparison

The official v0 archive SHA-256 is
`28d8f4f6d84331ff1e99ac3d68b917cf8174897a5c00171c5e493253eb1587f6`.
The deterministic rebuild uses a v1 identity because its compressed bytes do
not reproduce v0.

The extracted archives have identical path, directory, file, symlink, and
symlink-target lists: 138 directories, 1,904 files, and 571 symlinks. Of the
files, 1,510 are byte-identical and 394 differ. Every difference is
classified:

- 390 gate-built ELF files use the pinned build head and fixed November 2025
  release identity rather than v0's February 2026 build identity: 204 ELF32
  and 186 ELF64 files;
- the two amd64 shim files use the locked r151038/gcc 7 result; the i386 shims
  are byte-identical to v0;
- the two `libssp_ns.a` member payloads are byte-identical, but v0's archive
  headers contain 2026 timestamps and uid/gid 100/1 while v1 records
  deterministic zero metadata.

The v1 extracted-tree fingerprints are:

```text
files  f9596486042eb65c79e0ef43c49c9bf153d21286dc2bb53edf85422cad4b1b09
links  d7a60036439a32a53757dc280f901deb14081213c1bc6659126ad33fcb191338
```

## Contents, shims, and runtime

The archive contains the required headers, libraries, crt objects, both
architectures of `libssp_ns.a`, GSS libraries, and all four shim files. It
excludes every directory forbidden by the release plan, and every symlink
target resolves inside the extracted tree.

The matching repositories make the crt objects and `libssp_ns.a`
byte-identical across the two builds.  Their final archive SHA-256 values are:

```text
i386  crt1.o       6c5d9de8d17dcbd47be414b3bfd0cbee86cbe59df85f5770552661a50828a1e1
i386  crti.o       6cda35c99952478a0ef5c0e2f4aec9e016d6896dc131af9d41a5ba04f45088bb
i386  crtn.o       9e7ec1329e71a1c0f57577a5caae11ab0673dfc9a81c14fee67ddb49bc2c5c11
amd64 crt1.o       e240de00edf25865699e787d9fab4b7a2b849f056b8617e93b38cb4dabc2edcb
amd64 crti.o       2c4f6da68eb744fae164572fa97d4039051cae5a334ab7a75e09a09295ddb07c
amd64 crtn.o       84fbf89a741cc7fd00ff2f9730f9f65ead3018b5f95cfaec35d83b6ef33ce9f5
i386  libssp_ns.a  4875d9788435aac72fa004d31e77979c1d7e303e4d4c17967e1cc69c849f8e03
amd64 libssp_ns.a  264bf451960278d767ae9df5d623f63eef072b28cce31e8332e2423420a1c673
```

The shim SHA-256 values are:

```text
i386  libgcc_s.so.1     f62e7eb2e95a907840f1c26e1dbe312cca8d69cfc923175d43ce30d26bfad4b1
amd64 libgcc_s.so.1     7a30a34c0509ced78ecc66092912e277ddd3d64acfc92c988aa411df5770ec2f
i386  libssp.so.0.0.0   64e9888ed2bbbd6d8122361f5f5b13fd610501e57840ce62b0e686e2cde0d702
amd64 libssp.so.0.0.0   401fa18ecaabe70a068f8c8a7f2431ff8829c87f11a2132e43dae51bb3a8b35d
```

Helios `pvs` found `GCC_4.8.0` and `LIBSSP_1.0` in both architectures.
Exact-path native gcc 14 links against each 32-bit and 64-bit shim pair
produced valid Solaris executables. These shims are link-time compatibility
objects, not runtime replacements.

A Clang/LLD cross-linked amd64 smoke binary from the final archive recorded
exactly `libsocket.so.1`, `libpthread.so.1`, `libm.so.2`, and `libc.so.1`. It
ran on `helios-3.0.23976` and printed `illumos-sysroot-ok`.

## Remaining acceptance gaps

- Repeat the build on an independent r151038 builder when one is available.
- Run the manual hosted workflow from a pushed revision if hosted evidence is
  required for the release.
