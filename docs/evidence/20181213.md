# 20181213 release evidence

This file records lock-bound validation performed from 2026-08-27 through
2026-08-28.  Large kit artifacts and complete fingerprint manifests are
release artifacts; their trust anchor is `locks/toolchain.20181213.lock`.

## Builder and inputs

- Builder release: OmniOS r151030.
- Release base: `de6af22ae73ba8d72672288621ff50b88f2cf5fd`.
- Build head: `6265851cb0eea0e24c694164ca91635b4d414876`.
- Gate compiler: gcc 4.4.4; JDK: 8.
- Gate version: `sysroot/20181213-0-g6265851cb0ee`.
- Archive identity: `20181213-de6af22ae73b-v3`.
- Build driver SHA-256:
  `747f2ab8d2fb99f20798e583a4522018ef759e9d8dbdae34aeb484bf01343583`.
- Toolchain lock SHA-256:
  `37429e909ce85426d2ae282d1e0abe01a28deb214d51c33e0a89e0313b8e011a`.
- Rust: `1.94.1`; `mf2tar/Cargo.lock` SHA-256:
  `e380d06971a59c26b82f1cd824d216180f37b9e960af4ed4235f20dba12bd7f2`.
- Closed bins SHA-256: normal
  `18e82bace8481dca62586e4bdff7f6b44fc63b41443799929e4d4b2187e98535`,
  non-debug
  `da3ca1ea24972ba6a01169265d8d38a45789ed7cc45334bfe026c108b1d2ff56`.

The offline installer verified every exact requested package and the recorded
runtime closure before the build.  The gate-only input and checkout contained
no Rust source or Cargo manifest.

## Clean-build comparison

Two clean full nightlies (`final-build-20181213-12` and
`final-build-20181213-13`) completed without tool, build, or packaging errors
on the locked r151030 builder.  All eight normalized fingerprint manifests
match byte-for-byte.  Their complete `repo.redist` fingerprint is:

```text
directories                 772
files                     21820
links                         0
all files       bcb5f1e555e5596564d9c9e9fa3eb2028fd0d513e074721198ee7888e8707289
payloads        19ff75e64042d3fe3173d61e0df4f870d3541631a9dc6aadffbf4200ae34a18c
manifests       4a3521a3e61bf444cc66967c2badfe2735c4a32e6f96cdad8429ecc25e325a45
package actions 7f9db8e64a9fad1cb6912094bff42a5ca466dfc3ddaf944c06b4ca1c2785f52b
```

Linux assembly was run separately from each matching repository.  Both the
uncompressed and compressed outputs are byte-identical:

```text
tar     564d681705e2ee09f6ce9a130a4c6d8d6f53dd328396edbd061b177c46dede56
tar.gz  c03129229a770c27d1208bb4cc236f999d89f2e1d180285e2dcbe390d14004e1
```

The final repository was transferred to Linux and re-fingerprinted with all
four digests unchanged before assembly.  The quiescent builder package
closure was also recaptured: all 143 exact installed FMRIs match
`locks/toolchain.20181213.lock`.  No second independent r151030 builder is
currently available.

An earlier different-path diagnostic pair differed only in two generated
Javadoc files.  JDK 8 varied qualified versus unqualified method-link labels
and row ordering; the final normalizer canonicalizes both and compares against
the original bytes before deciding whether to rewrite.  The matching final
runs above prove that correction.

## Published-v2 comparison

The published v2 archive has SHA-256
`b09f1b240421228878cae608ab0b25a905900d1d5b70032b4aba15e0e7a8edc5`.
It is intentionally not reproduced under the v3 identity.  Exact extracted
tree comparison classified every path and file difference:

- v3 adds four GSS directories and the four gate-head GSS files they contain;
- v3 excludes two proprietary `llib-lm.ln` payloads containing the absolute
  historical build path `/ws/oldgate`, plus their two symlinks;
- of the common files, 1,490 match and 394 differ;
- the 394 differences are 206 ELF32 objects, 187 ELF64 objects, and
  `audit_uevents.h`;
- 393 ELF objects carry the retained branch head and fixed December 2018
  release identity instead of the published artifact's unavailable
  `ea9f1efc2e`/April 2020 identity; the header updates its copyright year.

The v3 extracted tree contains 137 directories, 1,888 files, and 572 symlinks.
Every symlink target resolves inside the tree.  Its normalized file and link
fingerprints are:

```text
files  139fa2d2f5041722f9c9764a221c2f449b6e7a497975e54094dca3ac8123cda1
links  81ee2b4384943a39d4363f87cf2d5d21842d0664fe4a1ef2a1c69735ea39a866
```

## Contents, shims, and runtime

The archive contains the required headers, crt objects, GSS libraries, and
four shim objects and excludes every directory forbidden by the release plan.
Both the 20181213 gate and published v2 archive lack `libssp_ns.a`, so v3
preserves that absence.

The matching repositories make both architectures of the crt objects
byte-identical across the two builds.  Their final archive SHA-256 values are:

```text
i386  crt1.o  ee3f3834f56a93a6d6cedd267dbb4b4a83070c9175d016d3fe068e92ca6ec894
i386  crti.o  4572563cc8c19d24181e6682e9743d74e72ed68a42504166a39dba4ad6bb29d0
i386  crtn.o  45b473759fb9e33ad2cfc899f82c42f5511cf0b9e05fd43ada08f36040852724
amd64 crt1.o  7d9477cf8b60a6c246edd245054acc91bf53cb35ce80109ddeee67f511dfa8a0
amd64 crti.o  3a535eeffc5d332df2712fb50e20e2320d350b3df6bf520cf202a3c03d0f722b
amd64 crtn.o  54d22b421fd323e45885aa2a2fecdf5d08997b429211e5a073ba7343a025c3e6
```

The shim SHA-256 values are:

```text
i386  libgcc_s.so.1     f62e7eb2e95a907840f1c26e1dbe312cca8d69cfc923175d43ce30d26bfad4b1
amd64 libgcc_s.so.1     edd5bcfd50ce40a917670ce385223f637db42af68dea7f3ffcae77152406c1e8
i386  libssp.so.0.0.0   64e9888ed2bbbd6d8122361f5f5b13fd610501e57840ce62b0e686e2cde0d702
amd64 libssp.so.0.0.0   9d136b14ce6b47f0117bba9fc7f27e20e26dc4f3ad9203ae7ea71306cd18e8ac
```

Native Helios `pvs` found `GCC_4.8.0` and
`LIBSSP_1.0` in both architectures.  Native 32-bit and 64-bit consumers linked
against the generated shims and both printed `illumos-shims-link-ok`.

A Clang/LLD cross-linked amd64 smoke binary from the final archive required
`libsocket.so.1`,
`libpthread.so.1`, `libm.so.2`, and `libc.so.1`.  It ran on
`helios-3.0.23976` and printed `illumos-sysroot-ok`.

## Remaining acceptance gaps

- Repeat the build on an independent r151030 builder when one is available.
